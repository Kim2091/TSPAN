# VapourSynth packaging

Ship the associated `vapourkit/*.vkfilter` with the VapourSynth script in source
and release packages. Update its parameters and docs whenever the script changes.
Use `package-vapoursynth.py`; models and TensorRT engines remain external.
